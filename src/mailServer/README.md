## Simple mail server that supports SMTP, POP3 and IMAP protocols.

### Compile and run:
1.
On `src` directory:
`javac ./mailServer/*.java`
`jar cmvf META-INF/MANIFEST.MF test.jar ./mailServer/`
`java -jar ./mailServer.jar`